// What is the output?

for (let j = 1; j < 6; j++) {
  console.log(
    String.fromCharCode(64 + j),
    String.fromCharCode(64 + j),
    String.fromCharCode(64 + j),
    String.fromCharCode(64 + j),
    String.fromCharCode(64 + j)
  );
}

// output
// A A A A A
// B B B B B
// C C C C C
// D D D D D
// E E E E E
